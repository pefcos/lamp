circuit recursion
    display arg
    lamp first arg.off
    power rec_if -first
    circuit rec_if
        switch arg arg.on
        power recursion on
    ground
ground

namespace recursion
switch recursion:arg (off (off (off (on (off (off on))))))
power recursion on